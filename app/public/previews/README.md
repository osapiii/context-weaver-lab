# Mode TOP プレビュー (非推奨)

カード Hero は **Nuxt UI Icon (`UIcon`)** + `useNavCardIcons` のカラー Iconify set
(flat-color-icons / vscode-icons) で表示しています。

写真・JPG プレビューは廃止しました。旧スクリプト (`download-nav-card-previews.sh` 等) は削除済みです。

アイコン差し替え: `app/composables/useNavCardIcons.ts`
